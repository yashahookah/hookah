# Конфигурация Amvera Cloud для Expo POS Adalya
# Проект в подпапке expo_pos_adalya, сборка через Dockerfile.adalya в корне

meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile.adalya

run:
  persistenceMount: /data
  containerPort: 80
