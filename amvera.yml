# Конфигурация Amvera Cloud для Expo POS
# Проект в подпапке expo_pos, сборка через Dockerfile в корне

meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile

run:
  persistenceMount: /data
  containerPort: 80
